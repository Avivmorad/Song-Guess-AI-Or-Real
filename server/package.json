{
  "name": "song-guess-ai-or-real-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "supabase": "supabase",
    "db:start": "supabase start",
    "db:stop": "supabase stop",
    "db:status": "supabase status",
    "db:reset": "supabase db reset --local",
    "db:lint": "supabase db lint --local --fail-on warning"
  },
  "devDependencies": {
    "supabase": "2.109.1"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
