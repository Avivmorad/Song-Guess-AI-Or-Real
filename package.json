{
  "name": "song-guess-ai-or-real",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx serve . -l 3000",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "serve": "14.2.4"
  },
  "engines": {
    "node": ">=20"
  }
}
